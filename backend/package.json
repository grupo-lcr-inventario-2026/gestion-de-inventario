{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "API de prueba con json-server para el sistema de gestion de deposito",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "0.17.4"
  }
}
